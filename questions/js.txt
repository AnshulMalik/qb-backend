
q = []
a = []

q.append('Inside which HTML element do we put the JavaScript? <%s>')
a.append('script')

q.append('What is a function defined inside another function (called the parent function), and has access to variables that are declared and defined in the parent function scope called?')
a.append('closure')

q.append('Which inbuilt functions can be used to empty an array in a single step/loop?')
a.append(['length', 'splice'])

q.append('What is the `$` used for?')
a.append('jquery')

q.append('$(document)._____() method begins to run code as soon as the Document Object Model (DOM) is loaded. Fill the blank')
a.append('ready')

q.append('________ JavaScript is basically a JavaScript methodology that seeks to overcome browser inconsistencies by separating page functionality from structure')
a.append('Unobtrusive')

q.append('What is used to represent not-a-number?')
a.append('Nan')

q.append(' ______ JavaScript is also called client-side JavaScript.')
a.append('Navigator')

q.append('__________ JavaScript is also called server-side JavaScript.')
a.append('LiveWire')

q.append('<_____> tag is an extension to HTML that can enclose any number of JavaScript statements.(Fill in the blank)')
a.append('script')

q.append('JavaScript entities start with _______ and end with _________.')
a.append(['Ampersand', 'semicolon'])

q.append(' ______method evaluates a string of JavaScript code in the context of the specified object.')
a.append('Eval')

q.append('JavaScript is interpreted by _________')
a.append('Client')

q.append('The _______ method of an Array object adds and/or removes elements from an array.')
a.append('Splice')

q.append('To enable data tainting, the end user sets the _________ environment variable.')
a.append('NS_ENABLE_TAINT')

q.append('Find one stateless protocol in the cube.')
a.append('HTTP')

q.append('How many WebSocket events are available?(in english)')
a.append('four')

q.append('Name all the websocket events in javascript.')
a.append(['open', 'close',  'message', 'error'])

q.append('Which attribute is used to specifies that the script is executed when the page has finished parsing (only for external scripts)?')
a.append('defer')

q.append('______ attribute is used to specify the character encoding used in an external script file.')
a.append('charset')

q.append('What is the parameter of the method getElementsbyTagName() if we need to get an image?')
a.append('img')

q.append('Which is the function used to call a function in every time duration?')
a.append('setInterval')

q.append('Function to get the DOM object in JavaScript?')
a.append('getElementbyId')

q.append('Which event handler is triggered when the user\'s mouse moves onto a link?')
a.append('onMouseOver')

q.append('Which event handler is triggered when the user\'s mouse moves away from a link?')
a.append('onMouseOut')

q.append('Inbuilt function to find the index of a particular string?')
a.append('indexOf')

q.append('Which is the command that is used to spoof the client user agent?')
a.append('setUserAgent')

q.append('Which object is the main entry point to all client-side JavaScript features and APIs?')
a.append('window')

q.append('Which identifier is used to represent a web browser window or frame?')
a.append('window')

q.append('What is the alternative function used in Node for load()?')
a.append('require')

q.append('The necessary globals of a node are defined under which namespace?')
a.append('process')

q.append('The object that looks to the thread that creates it is')
a.append('Worker')

q.append('Name the function throught which you can send data using a Worker object?')
a.append('postMessage')

q.append('Which event is fired when a document and all of its external resources are fully loaded and displayed to the user?')
a.append('load')

q.append('Name an alternative to the load event?')
a.append('readystatechange')

q.append('Which event can be fired on any scrollable document element?')
a.append('scroll')

q.append('The return type of getElementsByClassName() is')
a.append('NodeList')

for i in range(0,len(q)):
	if(type('str') == type(a[i])):
		print {"question": q[i], "answers": [a[i]]}
	else:
		print {"question": q[i], "answers": a[i]}
